# Base Application Configuration
spring.profiles.active=${SPRING_PROFILES_ACTIVE:local}

# Server Port (Binds to Render $PORT or default 8081)
server.port=${PORT:8081}

# JWT Authentication Security
app.jwt.secret=${JWT_SECRET:tripnest-local-dev-secret-2026-07-06-4g8k2m9p7q1x5z3v}
app.jwt.expiration-ms=${JWT_EXPIRATION_MS:86400000}

# CORS Allowed Origins
app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:5173,http://127.0.0.1:5173}

# SMTP / JavaMailSender Configuration
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:tripnest207@gmail.com}
spring.mail.password=${MAIL_PASSWORD:blhtgeycfenvkvxe}
spring.mail.properties.mail.smtp.auth=${MAIL_SMTP_AUTH:true}
spring.mail.properties.mail.smtp.starttls.enable=${MAIL_SMTP_STARTTLS_ENABLE:true}
spring.mail.properties.mail.smtp.starttls.required=true
app.mail.from=${MAIL_FROM:${MAIL_USERNAME:tripnest207@gmail.com}}

# Third-Party APIs & Caching
opentripmap.api.key=${OPENTRIPMAP_API_KEY:5ae2e3f221c38a28845f05b6bfe45a5e83cfd63984ab444a338f06f3}
wikipedia.user-agent=TripNest/1.0 (${spring.mail.username})
spring.cache.type=simple

# Cloudinary Integration Properties
cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME:}
cloudinary.api-key=${CLOUDINARY_API_KEY:}
cloudinary.api-secret=${CLOUDINARY_API_SECRET:}

# Trip Reminder Scheduler Configuration
app.tripnest.reminder.cron=${REMINDER_CRON:0 0 9 * * *}
app.tripnest.reminder.timezone=${REMINDER_TIMEZONE:Asia/Kolkata}

# Logging Defaults
logging.level.org.springframework.web=INFO
logging.level.com.tripnest.tripnest=INFO